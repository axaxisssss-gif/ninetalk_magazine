# 매거진 에디터 PRD v0.3

작성일: 2026-08-31
변경: 스타일 사양 확정(기존 발행본 기준), 포맷 단순화 유지

## 1. 개요

주제와 참고 링크를 넣으면 AI가 정해진 포맷의 매거진 초안을 쓰고, 결과물 화면에서 바로 텍스트와 이미지를 고쳐 완성한 뒤, HTML로 내려받아 직접 서버에 올리는 내부용 도구.

도구의 책임은 초안 생성 → 편집 → 파일 출력까지다. 발행과 호스팅은 도구 밖에서 FTP로 처리한다.

## 2. 사용 맥락

- 사용자: 내부 콘텐츠 담당자
- 작업은 PC에서, 결과물은 모바일 중심으로 보인다
- 한 번에 한 건씩 작업한다. 목록이나 팀 공유 저장소는 두지 않는다
- 완성본은 ZIP으로 내려받아 FileZilla로 서버에 업로드한다

## 3. 핵심 플로우

```
입력 화면 (주제 + 참고 링크 + 옵션)
  → [매거진 생성] → AI 초안 생성
  → 에디터 화면 (완성 형태로 렌더링, 그 위에서 바로 편집)
      ├ 텍스트 클릭 → 인라인 수정
      ├ 이미지 클릭 → 업로드 교체
      ├ 문단 추가 / 삭제 / 순서 변경
      ├ 컬러 변경
      └ [HTML 내려받기] → ZIP 다운로드
  → (FileZilla로 서버 업로드 — 도구 밖)
```

편집 화면과 미리보기를 나누지 않는다. 최종 결과물 형태 그대로 화면에 띄우고 그 위에서 직접 고친다.

작업 중인 매거진은 브라우저에 자동 저장된다. 다시 들어오면 마지막 상태에서 이어서 작업한다.

## 4. 콘텐츠 포맷

1. 제목
2. 인트로
3. 문단 1 … N — 중제목 + 본문 + 이미지
4. 아웃트로
5. 마무리 이미지

이게 전부다. 강조 박스, 비교표, 아이콘 그리드 같은 요소는 별도 컴포넌트로 만들지 않는다. 필요하면 이미지로 제작해 문단 이미지 자리에 넣는다.

- 중제목 번호는 시스템이 자동으로 매긴다. 문단을 지우거나 순서를 바꾸면 번호가 다시 계산된다
- 문단 이미지는 비워둘 수 있다
- 마무리 이미지도 선택이다

## 5. 화면 정의

### 5.1 입력 화면

| 필드 | 타입 | 필수 | 비고 |
|---|---|---|---|
| 주제 | 텍스트 | 필수 | 한 문장 |
| 참고 링크 | URL 목록 | 선택 | 추가/삭제, 최대 5개 |
| 추가 지시 | 텍스트 | 선택 | 톤, 강조할 관점 등 |
| 문단 수 | 선택 | 선택 | 자동(기본) / 3 / 4 / 5 / 6 |

이어서 작업할 내용이 있으면 상단에 "이어서 작업하기"를 노출한다. 새로 생성하면 이전 작업물이 덮이므로 실행 전에 확인을 받는다.

### 5.2 에디터 화면

| 대상 | 조작 |
|---|---|
| 제목/인트로/중제목/본문/아웃트로 | 클릭 후 인라인 수정, 포커스 아웃 시 반영 |
| 본문 내 강조 | 텍스트 선택 → 포인트 컬러 볼드 토글 |
| 이미지 영역 | 클릭 → 파일 업로드로 교체, 비어 있으면 업로드 안내 표시 |
| 문단 | 우측 핸들에서 위/아래 이동, 삭제, 아래에 문단 추가 |
| 컬러 | 상단 도구모음의 컬러 패널 — 프리셋 테마 선택 또는 슬롯별 HEX 입력 |

상단 도구모음: 컬러 · 저장 상태 · HTML 내려받기 · 처음부터 다시

## 6. 스타일 사양

기존 발행본에서 뽑은 값이다. 타이포와 레이아웃은 고정하고, 컬러는 조정할 수 있게 연다.

### 타이포그래피

폰트는 고딕 하나로 통일한다. Pretendard를 기본으로 하되, 사이트에서 이미 쓰고 있는 폰트가 있으면 그쪽에 맞춘다.

| 요소 | 크기 | 굵기 | 행간 |
|---|---|---|---|
| 제목 | 26px | 700 | 1.4 |
| 인트로 | 15px | 400 | 1.8 |
| 중제목 | 19px | 700 | 1.5 |
| 본문 | 15px | 400 | 1.8 |
| 아웃트로 | 15px | 400 | 1.8 |
| 이미지 캡션 | 13px | 400 | 1.6 |

문단 사이 간격 40px, 중제목과 본문 사이 12px, 본문과 이미지 사이 16px.

### 컬러

조정 가능한 슬롯은 다섯 개다. 기본값은 기존 발행본 기준.

| 슬롯 | 기본값 | 적용 대상 |
|---|---|---|
| 페이지 배경 | #FFFFFF | 전체 배경 |
| 본문 텍스트 | #333333 | 인트로, 본문, 아웃트로 |
| 보조 텍스트 | #767676 | 이미지 캡션 |
| 포인트 컬러 | #1B7FD4 | 제목, 중제목, 본문 내 강조 |
| 중제목 번호 | #8BC3EE | 중제목 앞 번호 |

- 컬러 패널은 프리셋 테마 목록을 먼저 보여주고, 펼치면 슬롯별 HEX 입력이 나온다
- 프리셋을 고른 뒤 개별 슬롯을 고쳐도 된다. 그 순간부터 "사용자 지정"으로 표시한다
- 배경과 본문 텍스트의 명도 대비가 부족하면 패널에 경고를 띄운다. 막지는 않는다
- 중제목은 번호와 텍스트의 색을 두 단계로 나눈다. 번호가 연하고 제목이 진하다

### 프리셋 테마

| 이름 | 배경 | 본문 | 포인트 | 번호 |
|---|---|---|---|---|
| 기본 (블루) | #FFFFFF | #333333 | #1B7FD4 | #8BC3EE |
| 아이보리 | #FAF8F3 | #33312C | #B07A3C | #DCC3A0 |
| 세이지 | #F4F6F2 | #2F332E | #4F7A55 | #A8C4AC |
| 연회색 | #F5F6F7 | #2E2E30 | #4A4A4E | #B4B4B8 |
| 차콜 | #2E2C28 | #EFEAE0 | #E0B65C | #7A6C4E |

### 레이아웃

- 콘텐츠 폭은 모바일 기준 고정 (430px, 좌우 여백 20px)
- PC에서는 콘텐츠 폭을 그대로 두고 배경색만 화면 전체로 확장, 콘텐츠는 가로 중앙 정렬
- 이미지는 콘텐츠 폭에 꽉 차게, 비율은 원본 유지
- 에디터 화면과 내보낸 HTML의 렌더링은 완전히 동일해야 한다. 편집 UI만 빠진다

## 7. 생성 로직

- Anthropic API(claude-sonnet-4-6) 호출
- 참고 링크는 API의 web_search 도구로 처리한다. 페이지를 직접 열어 읽는 방식이 아니므로, 링크와 그 주변 검색 결과를 참고해 초안을 쓰는 형태가 된다
- 응답은 JSON으로만 받는다

```json
{
  "title": "…",
  "intro": "…",
  "sections": [
    { "subhead": "…", "body": "…", "imageHint": "…" }
  ],
  "outro": "…"
}
```

- 제목은 질문형을 기본으로 한다 (기존 발행본 패턴)
- `imageHint`는 어떤 이미지를 넣으면 좋을지에 대한 한 줄 설명이다. 이미지 자리에 안내 문구로 표시해 업로드를 돕고, 이후 MCP 이미지 생성을 붙일 때 프롬프트 소스로 쓴다
- 분량 기준: 인트로·아웃트로 각 150~250자, 문단 본문 300~500자
- 생성 실패 시 입력값을 유지한 채 재시도

## 8. 임시저장

- 저장 슬롯은 하나다. 작업 중인 매거진 한 건만 브라우저에 보관한다
- 저장 시점: 텍스트 수정 후 포커스 아웃, 이미지 교체, 문단 구조 변경, 컬러 변경
- 저장 상태를 도구모음에 표시한다 (저장됨 / 저장 중)

| 키 | 내용 |
|---|---|
| `draft:current` | 본문 구조, 텍스트, 테마, 입력값 |
| `draft:img:{n}` | 이미지 데이터 (base64) |

- 저장소는 키당 5MB 제한이 있으므로 본문과 이미지를 분리해 저장한다
- 업로드 이미지는 클라이언트에서 최대 가로 1200px, JPEG 품질 0.8로 리사이즈한 뒤 저장한다

## 9. 내보내기 사양

`[HTML 내려받기]`는 ZIP 하나를 만든다.

```
magazine-{제목슬러그}-{YYYYMMDD}.zip
├ index.html
└ img/
    ├ 01.jpg
    ├ 02.jpg
    └ …
```

- 폴더 구조는 서버에서 이미 쓰고 있는 `index.html` + `img/` 형태를 그대로 따른다
- `index.html`은 이미지를 `img/01.jpg` 상대경로로 참조한다. 폴더째 올리면 그대로 동작한다
- CSS는 `index.html` 안에 인라인으로 넣는다. 별도 CSS 파일을 만들지 않는다
- 웹폰트는 CDN 링크로 넣는다
- 편집용 스크립트와 도구모음은 포함하지 않는다. 순수 정적 페이지로 나간다
- 이미지 파일명은 본문 등장 순서를 따른다. 마무리 이미지가 마지막 번호
- ZIP 생성은 JSZip을 사용한다

## 10. 범위 밖 (다음 단계)

- MCP 연동 이미지 생성 — 이번에는 업로드만. 이미지 소스를 `{ type: "upload" | "generated", ... }` 형태로 두어 나중에 갈아끼울 수 있게 한다
- 커버 디자인 생성, 푸터 자동 삽입
- 매거진 목록, 팀 공유, 권한 관리, 수정 이력
- FTP 자동 업로드
- 인스타 카드 변환

## 11. 남은 확인 항목

| 항목 | 비고 |
|---|---|
| 폰트 | 사이트에서 쓰고 있는 폰트 확인 후 확정 |
| 포인트 컬러 정확한 값 | 브랜드 블루 실제 HEX 확인 필요 |
| 푸터 | 기존 발행본에 있던 고지문·SNS·캐릭터 푸터를 넣을지 |
